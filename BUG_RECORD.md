# BUG_RECORD
## BUG-001：GPIO端口资源冲突
现象：LED、LCD、传感器外设工作异常，引脚电平错乱。
原因：多个驱动重复初始化配置同一个GPIO引脚。
处理：统一规划引脚功能，STM32CubeMX仅配置一次引脚，所有驱动不得重复修改GPIO。

## BUG-002：FreeRTOS任务时序延迟冲突
现象：传感器读数跳动、LCD刷新卡顿、串口响应延迟。
处理：使用vTaskDelayUntil维持固定周期，禁止忙等待；调整任务优先级、栈深度；拆分长耗时操作。

## BUG-003：串口多任务打印冲突
现象：串口日志乱码、文字交错缺字。
处理：FreeRTOS互斥量包裹完整串口发送逻辑，所有任务调用统一打印函数，禁止直接操作串口硬件。

## BUG-004：DHT11读取时序异常
现象：温湿度频繁读取失败、数值无效。
处理：严格匹配DHT11时序，设置最低采集间隔，读取时独占GPIO引脚，增加数据校验与超时判断。

## BUG-005：PA1 ADC电压采集异常
现象：转动电位器，ADC数值不变、电压换算异常。
处理：确认PA1配置为ADC模拟输入，核对ADC参考电压、采样时间、换算公式，用万用表校准。

## BUG-006：LCD刷新异常
现象：屏幕残影、闪烁、显示不全。
处理：仅单个任务管控LCD刷新，减少全屏刷屏，只刷新变动区域。

## BUG-007：FreeRTOS调度后任务不运行
现象：硬件初始化正常，开启调度器后任务无执行动作。
排查：FreeRTOS堆/栈容量、SysTick时钟配置；高优先级任务必须增加阻塞延时；开启栈溢出检测。

## BUG-008：build/.vscode误提交GitHub
处理：执行git rm --cached取消Git追踪，依靠.gitignore屏蔽，本地文件不会被删除。